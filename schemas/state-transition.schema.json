{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/schemas/state-transition.schema.json",
  "title": "Enki State Transition",
  "$comment": "Canonical transactional state-change payload. Executed transition records remain covered by enki-transition.schema.json for compatibility.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "transition_id",
    "transition_type",
    "subject",
    "domain",
    "from_states",
    "reason",
    "provenance_classification",
    "authority_class",
    "interpretation_version",
    "occurred_at"
  ],
  "properties": {
    "transition_id": {"type": "string", "minLength": 1},
    "transition_type": {
      "type": "string",
      "enum": [
        "CORRECTION",
        "REFINEMENT",
        "SUPERSESSION",
        "RETRACTION",
        "REVERSAL",
        "EXPANSION",
        "RESTRICTION",
        "CONFIDENCE_CHANGE",
        "CONTEXT_SHIFT",
        "MERGE",
        "SPLIT",
        "DEPRECATION"
      ]
    },
    "subject": {
      "type": "object",
      "additionalProperties": false,
      "required": ["subject_id", "subject_type", "namespace"],
      "properties": {
        "subject_id": {"type": "string", "minLength": 1},
        "subject_type": {"type": "string", "minLength": 1},
        "namespace": {"type": "string", "minLength": 1}
      }
    },
    "domain": {"type": "string", "minLength": 1},
    "from_states": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "enki-state-snapshot.schema.json"}
    },
    "to_states": {
      "type": "array",
      "items": {"$ref": "enki-state-snapshot.schema.json"},
      "default": []
    },
    "reason": {"type": "string", "minLength": 1},
    "evidence_ids": {
      "type": "array",
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1},
      "default": []
    },
    "provenance_classification": {"type": "string", "minLength": 1},
    "authority_class": {"type": "string", "minLength": 1},
    "interpretation_version": {"type": "string", "minLength": 1},
    "required_context": {
      "type": "array",
      "uniqueItems": true,
      "items": {"type": "string"},
      "default": []
    },
    "accepted_conflicts": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "OVERLAP",
          "BRANCH",
          "TARGET_STATE_ID_COLLISION",
          "AUTHORITY_CONFLICT",
          "CYCLE",
          "STALE_INPUT"
        ]
      },
      "default": []
    },
    "occurred_at": {"type": "string", "format": "date-time"},
    "metadata": {"type": "object", "default": {}}
  }
}
